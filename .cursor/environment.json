{
  "name": "all-electric — Late 2 The Party (static site)",
  "install": "python3 --version",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0 --directory docs",
      "description": "Serves the static site from docs/ at http://localhost:8000"
    }
  ],
  "ports": [
    {
      "name": "static site",
      "port": 8000
    }
  ]
}
